/** @type {import('next').NextConfig} */
const nextConfig = {
  // Hapus atau komentari bagian experimental yang tidak didukung
  // experimental: {
  //   allowedDevOrigins: [...]
  // },
  reactStrictMode: true,
}

module.exports = nextConfig